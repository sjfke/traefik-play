# 
# This configuration file enables the default "Welcome" page if there
# is no default index page present for the root URL.  To disable the
# Welcome page, comment out all the lines below. 
#
# NOTE: if this file is removed, it will be restored on upgrades.
#
<LocationMatch "^/+$">
    Options -Indexes
    ErrorDocument 403 /.noindex.html
</LocationMatch>

<Directory /usr/local/apache2/noindex>
    AllowOverride None
    Require all granted
</Directory>

Alias /.noindex.html /usr/local/apache2/noindex/index.html
Alias /poweredby.png /usr/local/apache2/icons/apache_pb3.png
Alias /system_noindex_logo.png /usr/local/apache2/icons/system_noindex_logo.png
